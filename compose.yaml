name: open-software-factory

services:
  postgres:
    image: postgres:18
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: open_software_factory
    volumes:
      - postgres-data:/var/lib/postgresql

volumes:
  postgres-data:
